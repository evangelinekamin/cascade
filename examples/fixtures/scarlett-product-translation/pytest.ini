[pytest]
# The product_translation tests are written as bare `async def test_*` with no
# @pytest.mark.asyncio decorator, mirroring the real backend's pyproject which
# sets asyncio_mode = "auto". Replicate it so the suite runs in the sandbox.
asyncio_mode = auto
